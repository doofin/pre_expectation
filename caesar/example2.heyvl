proc forty_two(x: Uint) -> (y: Uint)
    down requires ?(x == 41)
    down ensures ?(y == 42)
{
    y = x + 1;
}

proc main(x: UInt) -> (y: UInt)
{

  up negate;
    //x+1=>x+1 
    up assume x+1;

    //x+1
    x = x + 1;
    
    //x
    up assert x;
    //0
    up negate;
    //inf
}
